<nav class="flex justify-between items-center">
  <div>
    <%= link_to "Password Manager", root_path, class:"text-xl font-bold no-underline" %>
  </div>
  <ul class="list-none flex items-center gap-2">
    <% if user_signed_in? %>
      Currently signed in as: <%= current_user.email %>
      <li><%= link_to "Profile", edit_user_registration_path %></li>
      <li><%= button_to "Logout", destroy_user_session_path, method: :delete, class:"items-center" %></li>
    <% else %>
      <li><%= link_to "Sign in", new_user_session_path %></li>
      <li><%= link_to "Sign up", new_user_registration_path %></li>
    <% end %>
  </ul>
</nav>